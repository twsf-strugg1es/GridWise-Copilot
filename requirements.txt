# Pinned on purpose. The Docker fallback image and the judges' local
# reproduction must build the same tree we tested, and PuLP 4.0 removes
# PULP_CBC_CMD, which optimizer/solver.py uses.

fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
python-dotenv==1.2.3
python-multipart==0.0.32

openai==3.15.0
httpx==0.28.1

pulp==3.3.2

pytest==9.1.1
